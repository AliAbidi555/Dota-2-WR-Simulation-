# Optional: get a free key at https://www.opendota.com/api-keys
# Without a key you still get ~50,000 free calls/month
OPENDOTA_API_KEY=

# How many recent matches to fetch per player (default 20, max 500)
DEFAULT_MATCH_LIMIT=20
